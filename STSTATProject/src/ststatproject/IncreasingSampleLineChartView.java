/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package ststatproject;

/**
 *
 * @author Louis
 */

import java.util.ArrayList;
import javax.swing.JFrame;


public class IncreasingSampleLineChartView extends JFrame{
    /*
    private XYSeries series = new XYSeries("First");
    private XYSeriesCollection dataset = new XYSeriesCollection();
    
    public IncreasingSampleLineChartView(int n, ArrayList<Double> h, int N){
        
        
        for(int i = n; i <= N; i++){
            series.add(i, h.get(i-n));
        }
        
        dataset.addSeries(series);
        JFreeChart chart = ChartFactory.createXYAreaChart("Hypergeometric", "n", "Hypergeometric Distribution", dataset, PlotOrientation.HORIZONTAL, true, true, false);
        ChartPanel chartPanel = new ChartPanel(chart);
        chartPanel.setPreferredSize(new java.awt.Dimension(500, 270));
        setContentPane(chartPanel);
    }
    */
}
